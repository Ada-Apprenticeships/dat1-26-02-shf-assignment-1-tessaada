# Ways to not lose marks
Constraints:
- Ensure constraints are added to create schema
- If constraint not used, specify why if not obvious (i.e. use comments to explain)
- Specify `not null` for foreign key - unless referrential action to set foreign key to null if primary deleted.
- Ensure names match what is specified in the assignment

Consistent coding conventions:
- Capitalise SQL keywords